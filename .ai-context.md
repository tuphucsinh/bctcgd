# Project Context: Báo cáo Tài chính Gia đình (BCTCGD)

## 1. Vision & Purpose (Mục tiêu)
- **Problem:** Quản lý thu chi, nợ, tiền lãi và phân bổ tài sản của cấu trúc gia đình bị phân mảnh, khó nhìn tổng quan.
- **User:** Gia đình (Tài khoản riêng biệt: Hiếu, Ly. View "Gia đình" (GĐ) gộp dữ liệu cả hai).
- **Core Value:** Giao diện Dashboard Bento Grid hiện đại (Dark Mode "Premium"), tích hợp nhập liệu tức thời, báo cáo trực quan cho phép chuyển user 1 click (Hiếu/Ly/GĐ).
- **Các tính năng khác:** Theo dõi dư nợ gốc, chi phí lãi vay, phân bổ tài sản (BĐS, Cổ phiếu, Tiền mặt).

## 🛠️ Cấu trúc Database (v2) - CHI TIẾT
### 1. Phân loại Thu nhập & Chi phí (Categories)
- **Thu nhập (Income)**: 
  - *Chủ động*: Lương, Thưởng.
  - *Thụ động*: BĐS, Tài chính, Khác (Cần tính toán cho Gauge "Tự do tài chính").
- **Chi tiêu (Expense) - 10 mục**: 
  - Ăn uống, Di chuyển, Hóa đơn, Giao tế, Giải trí, Đồ dùng gia đình, Y tế, Học hành, Con cái, Khác.

### 2. Các loại Tài sản (Assets)
- **5 Nhóm chính**: Bất động sản (REAL_ESTATE), Tài chính (FINANCE), Crypto (CRYPTO), Vàng (GOLD), Khác (OTHER).
- **Trường dữ liệu**: Tên, Nhóm, Phân loại (LIQUID/FIXED), Số lượng, Giá mua, Đơn giá hiện tại, Tổng giá trị, Ghi chú, Ngân hàng (nếu có).
- **Logic**: 
  - Hỗ trợ **Bán một lần (Partial Asset Sale)**: Cho phép trừ số lượng lẻ, tự động tính lại `current_value` và giữ trạng thái `ACTIVE`. Nếu bán hết sẽ chuyển sang `SOLD`.
  - Checkbox "Trừ tiền mặt" (mặc định) khi thêm tài sản -> Tự động tạo giao dịch `ASSET_BUY`.

### 3. Quản lý Nợ & Cho vay (Debts)
- **Nợ (Borrow)**:
  - *Danh mục*: Nợ Ngân hàng, Cọc BĐS, Nợ khác.
  - *Phân loại*: Nợ ngắn hạn, Nợ dài hạn.
  - *Database Schema*: `id`, `name`, `debt_type` ('BORROW'/'LEND'), `amount` (Số gốc ban đầu), `remaining_amount` (Số nợ còn lại), `due_date`, `created_at`.
- **Cho vay (Lend)**:
  - *Dữ liệu*: Tên, Số tiền (`amount`), Ngày trả (dự kiến), Ghi chú.

### 4. Giao dịch (Transactions)
- **Loại**: INCOME, EXPENSE, DEBT_PAYMENT, ASSET_BUY, ASSET_SELL.
- **Dữ liệu**: Số tiền, Ngày, Người chi (Hiếu/Ly - Lưu DB dạng HIEU/LY), Danh mục, Ghi chú.
- **Logic Owner**: Chỉ còn 2 owner cứng `HIEU` và `LY`. View "Gia đình" (GĐ/all) là logic UI gộp dữ liệu của cả 2 thông qua `getOwnerFilter`.
- **Liên kết**: `is_principal_payment` (để phân biệt trả gốc và lãi nợ).

## 2. Tech Stack (Công nghệ)
- **Frontend:** Next.js 16 (App Router), React 19, Tailwind CSS v4.
- **State Management:** React Hooks (`useState`, `useContext` sau này cho Auth).
- **Database:** Supabase (PostgreSQL). Thao tác thông qua `@supabase/ssr` (Server Components/Actions) và REST Client.
- **Auth & Proxy**: Sử dụng logic chuyển đổi tài khoản đơn lẻ (tuphucsinh) làm master, bên trong chuyển đổi Profile (Hiếu/Ly) thông qua Cookie/Context. Logic Proxy/Edge Runtime tối ưu TTFB.
- **Unique Libs:** 
  - `framer-motion`: Hỗ trợ Animation cao cấp.
  - `sonner`: Component hiển thị thông báo toast.
  - `recharts` (Dự kiến): Cung cấp biểu đồ trực quan.
  - `lucide-react`: Bộ icon giao diện.
  - `shadcn/ui` (Phiên bản dựa trên **Base UI/MUI**, KHÔNG PHẢI Radix UI).

## 3. Architecture & Data Flow (Kiến trúc & Luồng dữ liệu)
- **Pattern:** Folder-by-feature (App Router), tách biệt logic ở `src/lib/`, UI components tái sử dụng ở `src/components/ui/`.
- **Primary Data Flow:** Client Component (Dashboard/Modal) ➔ Gọi Next.js Server Actions (sắp làm) ➔ Bypass RLS (nếu cần bằng Service Role) hoặc dùng Auth Client ➔ Database Supabase (các schema `transactions`, `debts`, `assets`, `categories`).
- **Core Value:** Giao diện Dashboard Bento Grid hiện đại (Dark Mode "Premium"), thiết kế **VisionOS-inspired** (Premium Glassmorphism), tối ưu diện tích (Compact), chuyển User nhanh bằng thanh **Toggle một chạm**.
- **Animation Philosophy:** Hạn chế chuyển động thừa. Ưu tiên giao diện tĩnh (Static) để tăng độ sang trọng. Chỉ dùng hiệu ứng "thở" (breathing) cho các cảnh báo cực kỳ quan trọng (ví dụ: vượt ngưỡng ngân sách).
- **Tính năng mở rộng:** Theo dõi dư nợ gốc, chi phí lãi vay, phân bổ danh mục đầu tư (BĐS, Chứng khoán, Tiền mặt).

## 4. Key Logic (Các Lỗi & Kinh nghiệm sinh tử - Đọc kỹ)
- **[CRITICAL] Component Base UI:** Thư viện giao diện đang cài đặt là **Base UI**. Khi dùng các component bọc (như `DialogTrigger`), **TUYỆT ĐỐI KHÔNG DÙNG** thuộc tính `asChild`. 
- **[NEW] Two-User Model Only:** Hệ thống đã loại bỏ hoàn toàn `JOINT` owner ở cả DB và Code. DB có constraint `CHECK (owner IN ('HIEU', 'LY'))`. Logic filter `all`/`joint`/`gd` từ URL đều được map về `['HIEU', 'LY']` qua hàm `getOwnerFilter`.
- **[NEW] Case-Sensitivity:** Dữ liệu `owner` trong DB luôn viết hoa (`HIEU`, `LY`). Logic code mapping (`helpers.ts`) chịu trách nhiệm chuẩn hóa từ lowercase sang uppercase.
- **[NEW] Accessibility & Console Errors:** Phải truyền trực tiếp `children` vào thuộc tính `render` của `DialogTrigger` (ép kiểu `as React.ReactElement`) để tránh lỗi lồng thẻ button gây đỏ console.
- **Compact UI Design:** Sử dụng bo góc `rounded-3xl` (24px) cho các thẻ lớn và `rounded-full` cho các nút điều hướng/header để tạo sự mềm mại. Giảm padding/gap (`p-4` đến `p-5`) để hiển thị nhiều thông tin.
- **[NEW] Deficit Warning UI:** Khi chi tiêu vượt thu nhập, thẻ so sánh không dùng animation pulsing mà dùng **Static Glow Shadow** (cam nhạt, lan tỏa 40px) và viền cam để cảnh báo một cách tinh tế nhưng rõ ràng.
- **Header Synchronization:** Các bộ chọn chức năng (Tháng/Năm, User Switcher) phải đồng bộ tuyệt đối về `padding`, `font-size (text-xs)`, `font-weight (semibold)` và `ring-1 border` để tạo sự cân bằng thị giác.
- **Deploy Vercel:** Dự án bắt buộc cần file `vercel.json` định tuyến `outputDirectory: ".next"`.
- **Database Schema:** Đã khởi tạo hoàn tất qua file `runSql.js`. Tương tác trực tiếp bằng URI Pooler (ipv4) thay vì DB PGBouncer mặc định của Supabase.
- **[NEW] Linting & Config:** Sử dụng ESLint 9+ với file cấu hình `eslint.config.mjs`. Các script rác/công cụ ở root được đưa vào `globalIgnores` để tránh lỗi build trên Vercel.
- **[NEW] Type System Strictness:** Mọi `any` type trong các component quan trọng (như biểu đồ) đã được chuyển sang strict types hoặc `Record<string, unknown>` để đảm bảo tính ổn định khi biên dịch.
- **[CRITICAL] Debts Schema Alignment:** Khi truy vấn hoặc thêm mới `debts`, phải dùng đúng tên cột: `amount` (thay cho original_principal), `remaining_amount` (thay cho remaining_principal) và `debt_type` (thay cho type/status). Logic `getDebts` không dùng filter `status` vì cột này không có trong schema gốc.

## 5. Transfer List (File ưu tiên - Đọc theo thứ tự)
*Khi chuyển giao hoặc tiếp tục dự án, Agent nên đọc các file này trước để nắm 90% logic hệ thống:*
1. `src/app/page.tsx` (Logic tổng quan Dashboard Bento Grid, User Switcher).
2. `src/components/ui/transaction-modal.tsx` (Core logic nhập liệu, Pattern dùng Base UI chuẩn).
3. `supabase_schema.sql` (File schema gốc định nghĩa Database).
4. `src/lib/supabase.ts` (File khởi tạo Singleton của Supabase Client/Admin).
5. `vercel.json` (Trái tim của việc Deploy).

## 6. Team Rules & Conventions (Quy tắc)
- **Naming:** `camelCase` cho hàm/biến, `PascalCase` cho Component, `snake_case` cho database columns.
- **Styling:** Ưu tiên **UI Premium** (Glassmorphism với `backdrop-blur-md`, gradient borders tinh tế `white/10` đến `white/20`, shadow `shadow-2xl` hoặc glow shadow đặc trưng cho trạng thái cảnh báo).
- **Navigation:** Mobile Bottom Navbar sử dụng nhãn tiếng Anh (e.g., **"Home"** thay vì "Tổng quan") và thiết kế **Ultra-compact** (giảm padding tối đa) để tối ưu không gian hiển thị.
- **Coding:** TypeScript strict mode, ES6+, Lambda, Shorthand logic. Phân tách rõ Client (`"use client"`) và Server components.
- **Git:** Commit message theo chuẩn Conventional Commits. Repo: `https://github.com/tuphucsinh/bctcgd`.
- **Code Format:** Chỉ cung cấp `diff`. Cấm Yapping.
- **Communication:** Ngôn ngữ Tiếng Việt, xưng hô Anh/Em. Phản hồi ngắn gọn.
- **Logic Check:** Luôn tự kiểm tra lỗi Typo và RLS policy trước khi đề xuất sửa logic Data Flow.

## 7. Current Progress (Tiến độ)
- **Done:** 
  - Khởi tạo repo, thiết lập DB Supabase hoàn chỉnh.
  - Hoàn thiện UI Dashboard Bento Grid cao cấp (Dark Mode, VisionOS style).
  - Tích hợp thành công `recharts` (Pie/Area Chart) vào Dashboard & Asset Page.
  - Xây dựng trang Chi tiết tài sản/Công nợ/Thu nhập (Transactions) với logic lọc User (Hiếu/Ly/GĐ).
  - Loại bỏ hoàn toàn `JOINT` owner, thiết lập Check Constraint bảo vệ dữ liệu.
  - Triển khai logic chuẩn hóa Case-sensitive cho `owner` thông qua Server Actions.
  - Deploy ổn định trên Vercel, sạch lỗi Linting và Type errors.
- **To-do:** 
  - Xây dựng logic Trả nợ (Repayment): Khi trả nợ sẽ tạo bản ghi `transaction` loại `DEBT_PAYMENT` và trừ `remaining_amount` trong bảng `debts`.
  - Tối ưu hóa Performance cho view "Gia đình" khi dữ liệu lớn (Indexing `owner` & `date`).
  - Hoàn thiện báo cáo năm (Annual Report) trực quan hóa tăng trưởng tài sản ròng.
